name: goability-backend
runtime:
  image: node:20

commands:
  build:
    - cd backend-mysql
    - npm install
  start: cd backend-mysql && npm start

port: 5000

healthcheck: /api/health
auto_deploy: true
